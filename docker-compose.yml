services:
  streamlit-app:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "${PORT}:8501"
    env_file:
      - .env
    networks:
          - app_network

networks:
  app_network:
    driver: bridge