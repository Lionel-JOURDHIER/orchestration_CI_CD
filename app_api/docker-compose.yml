services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${FASTAPI_PORT:-8000}:8080"
    # volumes:
    #   - ./app_api:/app
    environment:
      - FASTAPI_PORT=8080
    restart: always